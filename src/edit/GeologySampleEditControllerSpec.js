'use strict';
require('should');

describe('GeologicalSampleEditController', function () {
  var $scope, $controller, $routeParams, GeologicalSample;

  // Set up mocks
  before(function () {
    $scope = {};
    $routeParams = {};
    GeologicalSample = {};
    $controller = function () {};
  });

  describe('#initFormula', function () {
    // no test...
  });
});
